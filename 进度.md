# 进度记录

> 规则：后续每一步工程操作都同步记录到本文件，包含目的、改动文件、验证和推送状态。

## 2026-06-15

### 1. 初始化并推送工程

- 操作：在 `dingtalk-game` 目录初始化 Git 仓库。
- 远端：`https://github.com/nmwllwmn/keji.git`
- 分支：`main`
- 提交：`226cc6a Initial commit`
- 内容：提交初始 HTML 页面。
- 状态：已推送。

### 2. 修复 GitHub Pages 入口和坏链路

- 问题：Pages 根路径没有 `index.html`，登录后跳转的 `workbench.html` 在线上是不完整文件。
- 操作：补充入口页，修复工作台和消息页导航中的缺页链接。
- 提交：`cc4ad31 Fix Pages entry and broken navigation`
- 状态：已推送。

## 2026-06-16

### 3. 从 GitHub 强制拉取并清理本地差异

- 操作：执行 `git fetch origin main`、`git reset --hard origin/main`、`git clean -fd`。
- 目的：删除本地和 GitHub 对不上的未跟踪内容。
- 当前远端提交：`4235d53 Add ARG landing page and desktop clue archive`
- 状态：本地已和远端对齐。

### 4. 替换顾路工牌 3D 页面

- 来源：`c:\Users\Administrator\Downloads\1\code (1).txt`
- 目标文件：`badge-template.html`
- 操作：用用户提供的新 3D 工牌 HTML 整体替换旧版。
- 验证：执行 `git diff --check`，清理行尾空格后通过。
- 提交：`f37a785 Update Gu badge 3D page`
- 状态：已推送。

### 5. 新增介绍页

- 文件：`intro.html`
- 内容：新增“南洄晚报 - 特别报道”页面。
- 验证：检查标题、脚本、图片引用和远端状态。
- 提交：`09bf9ef Add intro report page`
- 状态：已推送。

### 6. 将介绍页替换为首页

- 操作：用 `intro.html` 覆盖旧首页 `index.html`。
- 二维码：
  - 微信公众号：`wechat-official.jpg`
  - 小红书：`xiaohongshu.jpg`
- 验证：确认首页标题为“南洄晚报 - 特别报道”，二维码路径已改为实际图片。
- 提交：`87074ad Replace homepage with intro page`
- 状态：已推送。

### 7. 新增工牌投递彩蛋测试页

- 文件：`badge-drop-test.html`
- 功能：
  - 支持拖拽或选择工牌 HTML 文件。
  - 读取本地文件内容，不上传。
  - 校验 `GL-0606`、`顾□□□`、`Recovered Staff Credential`。
  - 成功后写入 `localStorage.nh_badge_drop_easter_egg = unlocked`。
  - 展示彩蛋剧情。
- 首页入口：在 `index.html` 和 `intro.html` 的“开始调查”下方加入“工牌投递测试”链接。
- 验证：执行 `git diff --check`，通过。
- 提交：`9d8edb4 Add badge drop easter egg test`
- 状态：已推送。

### 8. 支持 Base64 加密工牌上传校验

- 文件：`badge-drop-test.html`
- 加密/解密规则：
  - Python 加密：`b64encode(html.encode("utf-8"))`
  - JS 解密：`atob()` 转字节数组，再用 `TextDecoder("utf-8")`
- 上传支持：
  - 明文 `.html` / `.htm`
  - Base64 文本 `.txt`
- 解密后判断字段：
  - ID：`GL-0606`
  - 姓名：`顾□□□`
  - 公司：`南洄科技有限公司`
  - 工牌类型：`Recovered Staff Credential`
- 新增按钮：`下载加密样本`
- 验证：
  - `git diff --check` 通过。
  - 使用本地 `badge-template.html` 生成 Base64 后解码，四个字段均匹配。
- 提交：`c0bbe19 Support encoded badge upload validation`
- 状态：首次推送因网络失败，后续重试成功推送。

### 9. 创建进度记录文件

- 文件：`进度.md`
- 操作：把当前工程关键步骤整理为可追踪进度记录。
- 提交：`db4fb86 Add progress log`
- 状态：已推送。

### 10. 首页报道加入家属口述并更新登录口令

- 文件：
  - `index.html`
  - `intro.html`
  - `login.html`
  - `进度.md`
- 操作：
  - 在首页报道正文中加入“家属口述摘录”模块。
  - 通过“六月六”“两个零夹着两个六”等口述信息隐晦提示登录口令 `0606`。
  - 将登录页原 `20240615` 改为 `0606`。
  - 将错误反馈和隐藏提示改为四位旧口令语境，避免和旧的 8 位日期提示冲突。
- 验证：
  - `git diff --check` 通过。
  - 检查 `index.html`、`intro.html`、`login.html`，确认旧的 `20240615`、`今天的日期`、`YYYYMMDD` 提示已移除。
  - 确认登录页校验口令为 `0606`。
- 提交：`e674175 Hide login password in family quote`
- 状态：已推送。

### 11. 调整调查身份与家属口述措辞

- 文件：
  - `index.html`
  - `intro.html`
  - `进度.md`
- 操作：
  - 将“南洄科技新任信息安全专员”改为“南洄市新任信息安全专员”。
  - 将入口说明同步为“南洄市信息安全专员”。
  - 将家属口述改为“常把重要的密码都用生日重复用”，让 `0606` 线索更明确。
- 验证：
  - `git diff --check` 通过。
  - 检查 `index.html`、`intro.html`，确认旧身份“南洄科技新任信息安全专员”和旧口述“重要的小数字”已无残留。
- 提交：`cf2b6a7 Refine investigator identity and password quote`
- 状态：已推送。

### 12. 修正家属口述采访日期

- 文件：
  - `index.html`
  - `intro.html`
  - `进度.md`
- 操作：将“顾某亲属，6月7日电话采访记录”修正为“顾某亲属，6月6日电话采访记录”。
- 原因：事件和生日线索均指向 6月6日，采访日期应保持同日口径。
- 验证：
  - `git diff --check` 通过。
  - 检查 `index.html`、`intro.html`，确认页面展示为“6月6日电话采访记录”。
- 提交：`6332bfc Fix family quote interview date`
- 状态：已推送。

### 13. 弱化家属口述中的密码线索

- 文件：
  - `index.html`
  - `intro.html`
  - `进度.md`
- 操作：删除“门锁、便签、旧手机备忘录里都能看到那组生日数，两个零夹着两个六”这句过于直白的口述。
- 保留：家属口述仍保留“重要的密码都用生日重复用”和“这个日子出了事”，让 `0606` 线索更自然。
- 验证：
  - `git diff --check` 通过。
  - 检查 `index.html`、`intro.html`，确认页面中已无“门锁”“便签”“旧手机”“两个零”等直白提示。
- 提交：`e2d3204 Soften family quote password clue`
- 状态：已推送。

### 14. 恢复页面链接新窗口打开

- 文件：
  - 所有当前 HTML 页面
  - `进度.md`
- 操作：
  - 为实际 `.html` 页面链接统一添加 `target="_blank"`。
  - 同时添加 `rel="noopener noreferrer"`。
  - `href="#"` 的占位链接保持不变。
- 原因：保持和之前一样的使用方式，每次打开新窗口，避免当前页面被覆盖。
- 验证：
  - `git diff --check` 通过。
  - 扫描所有 `.html` 实际页面链接，确认均包含 `target="_blank"`。
- 状态：待提交推送。

### 15. 重做登录页为南洄紫色品牌界面

- 文件：
  - `login.html`
  - `进度.md`
- 操作：
  - 将登录页品牌从“钉钉”改为“南洄”。
  - 图标从“钉”改为“洄”。
  - 页面底色改为紫蓝渐变，并加入左侧品牌宣传区、浮动符号、右侧白色登录卡。
  - 保留原登录逻辑，密码仍为 `0606`。
- 参考：用户提供的紫色南洄科技登录页示意图。
- 验证：
  - `git diff --check` 通过。
  - 检查 `login.html`，确认“钉钉/钉”品牌文案已替换为“南洄/洄”。
  - 确认登录口令仍为 `0606`。
  - 登录成功后改为新窗口打开 `workbench.html`。
- 提交：`3a7cd26 Redesign login page for Nanhui brand`
- 状态：已推送。

### 16. 还原登录卡结构并保留南洄品牌样式

- 文件：
  - `login.html`
  - `进度.md`
- 操作：
  - 撤回上一版海报式大布局，恢复原来的居中登录卡结构。
  - 保留参考图里的品牌方向：紫色底、南洄品牌、图标“洄”。
  - 保留登录口令 `0606`。
  - 保留登录成功后新窗口打开 `workbench.html`。
- 原因：参考图只用于图标和品牌样式，不应把登录页改成整张参考图的构图。
- 验证：
  - `git diff --check` 通过。
  - 检查 `login.html`，确认恢复居中登录卡结构，且保留“南洄/洄”、紫色背景、`0606` 和新窗口打开工作台。
- 提交：`b584935 Restore login card with Nanhui styling`
- 状态：已推送。

### 17. 编写网页解谜游戏完整剧本

- 文件：
  - `剧本.md`
  - `进度.md`
- 操作：
  - 基于南洄科技园 C 座坠落报道，设计《回声工牌》网页解谜游戏剧本。
  - 明确玩家身份为南洄市新任信息安全专员。
  - 围绕南洄工作台的消息、文档、AI 表格、AI 听记、工作台、通讯录、会议、日历、待办、DING、添加功能设计线索。
  - 将顾路工牌 `GL-0606` 设计为主线钥匙。
  - 设计玩家 `TMP` 临时工工牌和隐藏剧情。
  - 设计普通结局、隐藏结局、完美结局、彩蛋结局。
  - 写出完整长线索路线和漫剧分集结构。
- 验证：
  - `git diff --check` 通过。
  - 检查 `剧本.md`，确认 TMP 被定义为独立彩蛋，不影响正式四结局。
  - 检查 `剧本.md`，确认已加入“血迹催命文”误导支线。
  - 检查编号线索数量，当前编号条目超过 100 条。
- 状态：已提交推送。

### 18. 重构剧本 v2：误导线和 TMP 独立彩蛋

- 文件：
  - `剧本.md`
  - `进度.md`
- 操作：
  - 修正 TMP 设定：TMP 工牌不再是主线，不影响正常四个结局。
  - 增加独立“临时工牌结局”，只给 TMP 工牌拥有者触发。
  - 新增“上属催方案消息变血迹催命文”的误导支线。
  - 明确该血迹催命文是污染视图/错误判断，不影响结局。
  - 将主线线索扩展到 136 条。
  - 重写四正式结局：普通、隐藏、完美、彩蛋。
- 验证：待执行。
- 状态：待提交推送。

### 19. 编写 60s 宣传短剧剧本

- 文件：
  - `短剧.md`
  - `进度.md`
- 操作：
  - 新增《回声工牌》60 秒竖屏宣传短剧脚本。
  - 按短剧预告片节奏拆分为 0-3s、3-8s、8-14s 等时间段。
  - 写入画面、镜头、音效、屏幕文字、旁白、转场和结尾钩子。
  - 结合南洄工作台、顾路工牌、0606、补交设备、DING、消息污染视图等既有元素制造悬疑。
  - 明确宣传片不透露真相、不透露四个正式结局、不透露 TMP 彩蛋结局。
- 目的：用于 60s 宣传短剧制作，吸引玩家进入游戏，同时避免提前透题。
- 验证：
  - `git diff --check` 通过。
  - 扫描 `短剧.md` 关键词，确认未写入真相揭示、正式结局内容或 TMP 主线剧透。
  - 检查脚本结构，已包含 60s 时间分段、镜头语言、音效和 15s 备用剪辑版。
- 状态：已提交推送。

### 20. 按参考图重做工作台侧边栏并预留功能壳

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 按用户提供的侧边栏参考图重做工作台导航结构，调整宽度、圆角、浅蓝底色、红点和徽标样式。
  - 将左侧功能入口按“消息、文档、AI 表格、AI 听记、工作台、通讯录、会议、日历、待办、DING、添加”的顺序重排。
  - 将工作台主体拆为主线功能入口、证据页面入口、今日异常摘要和右侧拖拽/状态区。
  - 新增 `module-shell.html` 作为通用功能壳，占位 AI 表格、AI 听记、会议、待办、DING、添加、旧版访客记录、设备归还、归档提交等页面。
  - 保留“工牌核验”入口，后续继续接入顾路工牌和拖拽解密逻辑。
  - 目前只做 UI 和入口结构，不写具体证据内容。
- 目的：让工作台布局更贴近参考图，同时和剧情剧本的功能模块一一对应。
- 验证：
  - `git diff --check` 通过。
  - 本地静态服务器访问 `http://127.0.0.1:4173/workbench.html` 返回 200。
  - 本地静态服务器访问 `http://127.0.0.1:4173/module-shell.html?module=aiTable` 返回 200。
  - 检查 `workbench.html`，确认新增功能入口均保留 `target="_blank"` 和 `rel="noopener noreferrer"`。
  - Playwright 截图未完成：本机缺少 Playwright Chromium，下载浏览器包超时。
- 状态：已提交推送。

### 21. 按参考代码重构工作台为桌面办公布局

- 文件：
  - `workbench.html`
  - `进度.md`
- 操作：
  - 读取用户提供的 `code (1).txt`，参考其桌面端工作台结构。
  - 将工作台改为深色 200px 侧边栏、顶部标签栏、搜索区、蓝色 banner、常用应用、全部应用、待办、待处理、日历、日程、公告和工牌拖入窗口布局。
  - 保留南洄品牌图标“洄”和现有 lucide 图标风格，不直接照搬参考代码里的图标。
  - 保留所有已有功能入口：消息、文档、AI 表格、AI 听记、通讯录、会议、日历、待办、DING、添加、工牌核验、旧版访客记录、设备归还、归档提交。
  - 所有实际页面链接继续使用新窗口打开。
  - 只优化 UI 和功能入口结构，具体证据内容仍不接入。
- 目的：让工作台更接近用户提供的参考工作台，同时服务剧本里的解密功能路线。
- 验证：
  - `git diff --check` 通过。
  - 本地静态服务器访问 `http://127.0.0.1:4173/workbench.html` 返回 200。
  - 检查 `workbench.html` 链接，确认实际页面入口均保留 `target="_blank"` 和 `rel="noopener noreferrer"`。
  - 确认工作台保留南洄“洄”标识和 lucide 图标风格。
- 状态：待提交推送。

### 22. 继续贴近参考工作台基础元素

- 文件：
  - `workbench.html`
  - `进度.md`
- 操作：
  - 根据用户要求，工作台基础元素尽量沿用用户提供的参考代码结构。
  - 侧边栏分组调整为“消息、工作、智能、应用”，更接近参考模板的信息层级。
  - 顶部标签改为“工作台、消息、文档、通讯录”，贴近参考工作台常规入口。
  - banner 数据恢复为四组：未读消息、待办事项、待审批、今日日程。
  - 常用应用保留五个基础入口：审批、访客记录、设备归还、归档、DING。
  - 右侧恢复为参考模板的日历、今日日程、公告通知三块，不额外加入独立卡片。
  - 工牌核验继续保留在全部应用中，不影响参考模板的主体节奏。
- 目的：不再大幅自创结构，尽量使用用户提供的工作台基础元素，只替换为南洄和剧情文字。
- 验证：
  - `git diff --check` 通过。
  - 本地静态服务器访问 `http://127.0.0.1:4173/workbench.html` 返回 200。
  - 检查 `workbench.html` 链接，确认实际页面入口均保留 `target="_blank"` 和 `rel="noopener noreferrer"`。
  - 确认常用应用、全部应用、我的待办、待我审批、日历、今日日程、公告通知均保留参考模板基础元素。
- 状态：已提交推送。

### 23. 优化 60s 悬疑宣传短剧脚本 v2

- 文件：
  - `短剧.md`
  - `进度.md`
- 操作：
  - 重写《回声工牌》60s 宣传短剧为 v2。
  - 明确全片不露任何人物正脸，只使用手、背影、屏幕反光、工牌、监控画面、空椅子等视觉替代。
  - 增强悬疑宣传片镜头语言：J-cut、匹配剪辑、遮挡转场、甩镜、卡帧闪白、静音断崖、同构镜头二次污染。
  - 优化 0-60s 分镜节奏，前三秒强化“人已坠落但系统仍显示完成待办”的反常识钩子。
  - 新增无露脸拍摄规则、高悬疑剪辑手法、声音设计、15s 强钩子版。
  - 保持不透露真相、不透露四个正式结局、不透露 TMP 彩蛋结局。
- 目的：让短剧宣传片更适合拍摄成高悬疑感、不露脸、强引流的视频预告。
- 验证：
  - `git diff --check` 通过。
  - 检查 `短剧.md`，确认已明确“全片不露任何人物正脸”。
  - 检查 `短剧.md`，确认已加入 J-cut、匹配剪辑、遮挡转场、甩镜、卡帧闪白、静音断崖等镜头/剪辑手法。
  - 检查 `短剧.md`，确认未透露真相、正式结局或 TMP 彩蛋结局。
- 状态：已提交推送。

### 24. 按用户新开场重写短剧 v3

- 文件：
  - `短剧.md`
  - `进度.md`
- 操作：
  - 按用户设想重写《回声工牌》60s 悬疑宣传短剧 v3。
  - 开头改为南洄科技 C 座远景，一个人影从高楼坠下，但不拍落地细节和正脸。
  - 紧接记者韩沛在警戒线前播报事件，镜头只拍半侧身、话筒、记者证，不露完整正脸。
  - 警戒线后加入紫色工牌，作为后续南洄工作台和 GL-0606 主线入口的视觉钩子。
  - 后续剧情围绕韩沛报道声、登录页、工作台红点、紫色工牌、郑明消息误导、证据连环转场、旧记录弹窗推进。
  - 保持全片不露脸、不透露真相、不透露正式结局、不透露 TMP 彩蛋结局。
  - 新增对应的无露脸拍摄规则、剪辑手法、台词版、声音设计和 15s 强钩子版。
- 目的：让宣传短剧更符合用户想象中的新闻现场开场，并增强工牌作为第一视觉证据的悬疑吸引力。
- 验证：
  - `git diff --check` 通过。
  - 检查 `短剧.md`，确认已加入“远景坠落人影、记者韩沛、警戒线、紫色工牌”开场。
  - 检查 `短剧.md`，确认韩沛和现场人员均不露完整正脸。
  - 检查 `短剧.md`，确认未透露真相、正式结局或 TMP 彩蛋结局。
- 状态：已提交推送。

### 25. 统一公司名称为南洄科技有限公司

- 文件：
  - `intro.html`
  - `index.html`
  - `短剧.md`
  - `剧本.md`
  - `进度.md`
- 操作：
  - 将“南洄智云科技有限公司前员工”统一改为“南洄科技有限公司前员工”。
  - 将项目文本中的“南洄智云”“智云科技”等旧称统一替换为“南洄科技有限公司”或“南洄科技”。
  - 同步更新首页报道、短剧播报词和剧本设定中的公司名称口径。
- 目的：避免玩家看到多个公司名，统一事件主体为“南洄科技有限公司”。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `intro.html`、`index.html`、`短剧.md`、`剧本.md`，确认正文已统一为“南洄科技有限公司”。
  - 搜索确认旧称只保留在 `进度.md` 的本次操作记录中，用于说明替换来源。
- 状态：待提交推送。

### 26. 调整首页同日简讯展示顺序

- 文件：
  - `intro.html`
  - `index.html`
  - `进度.md`
- 操作：
  - 将同日简讯展示顺序调整为 16:45 在上，18:30 在下。
  - 16:45 对应“南洄大学北桥片区夜间临时管制，校方称与雨水设施巡检有关。”
  - 18:30 对应“南洄科技园部分企业网络认证服务短时异常，园区信息办称已恢复。”
  - 两个页面保持一致。
- 目的：按用户要求调整两条简讯的展示顺序。
- 验证：
  - `git diff --check` 通过。
  - 检查 `intro.html`，确认 16:45 在上、18:30 在下。
  - 检查 `index.html`，确认 16:45 在上、18:30 在下。
- 状态：待提交推送。

### 27. 更改报道标题为南洄科技园 C 座

- 文件：
  - `intro.html`
  - `index.html`
  - `短剧.md`
  - `剧本.md`
  - `进度.md`
- 操作：
  - 将首页报道标题从“南洄科技C座”改为“南洄科技园C座”。
  - 同步更新剧本和短剧中“南洄科技 C 座坠落”相关口径为“南洄科技园 C 座坠落”。
- 目的：按用户要求将标题统一为“一名24岁男子从南洄科技园C座坠落 / 警方排除刑事案件”。
- 验证：
  - `git diff --check` 通过。
  - 检查 `intro.html` 和 `index.html`，确认主标题已改为“一名24岁男子从南洄科技园C座坠落 / 警方排除刑事案件”。
  - 检查 `短剧.md` 和 `剧本.md`，确认相关口径已同步为“南洄科技园 C 座坠落”。
- 状态：已提交推送。

### 28. 调整调查入口说明文案

- 文件：
  - `intro.html`
  - `index.html`
  - `进度.md`
- 操作：
  - 将“发现其电脑中存在大量加密文件和异常通讯记录”改为“发现其电脑中工作台存在大量异常信息”。
  - 两个首页入口页面保持一致。
- 目的：让调查入口更聚焦南洄工作台异常，而不是加密文件和通讯记录。
- 验证：
  - `git diff --check` 通过。
  - 检查 `intro.html` 和 `index.html`，确认旧句已替换为“发现其电脑中工作台存在大量异常信息”。
- 状态：已提交推送。

### 29. 重构剧本为双层工作台结构

- 文件：
  - `剧本.md`
  - `线索.md`
  - `短剧.md`
  - `进度.md`
- 操作：
  - 将游戏主结构改为“表层工作台 -> 工牌审核入口 -> 真实工作台”。
  - 明确玩家第一次登录后看到的是看似完全正常、所有人和和气气的表层工作台。
  - 新增最坏结局“草率的笨蛋”：玩家只调查表层系统并草草归档，导致 `GL-0606` 后续从回收池清空。
  - 将顾路工牌从开局主线钥匙调整为后段发现“工牌审核入口”后的真实工作台入口，并影响隐藏真实结局。
  - 重构正式结局为：普通结局、彩蛋结局、真实结局、隐藏真实结局；TMP 保持独立临时工牌彩蛋。
  - 重写 `线索.md` 的状态字段、推进路线和结局判断条件，加入 `surfaceWorkbenchChecked`、`badgeAuditEntranceFound`、`realWorkbenchUnlocked`、`signedOffTooEarly` 等状态。
  - 调整 `短剧.md`，弱化“拖入工牌”作为宣传片唯一钩子，改为强调“表层工作台看似无异常”的悬疑反差。
- 目的：按用户最新设想重构剧情，让真正游戏从发现隐藏工牌审核入口后展开，同时保留工牌支线对隐藏真结局的影响。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `剧本.md`、`线索.md`、`短剧.md`，确认已出现“表层工作台”“真实工作台”“工牌审核入口”“草率的笨蛋”“隐藏真实结局”等新结构关键词。
  - 搜索正文，确认已无“顾路工牌是主线钥匙”和“把工牌拖进去”的旧宣传钩子。
- 状态：待提交推送。

### 30. 修正入口逻辑为顾路账号退出后工牌登录

- 文件：
  - `剧本.md`
  - `线索.md`
  - `短剧.md`
  - `进度.md`
- 操作：
  - 按用户修正，将玩家初始登录对象明确为“顾路的工作台”，不是玩家自己的工作台。
  - 将原“添加页/工牌审核入口”改为右上角顾路头像菜单里的“退出登录”。
  - 设计退出登录后进入新的“南洄凭证复核登录 / 工牌登录”界面，需要刷入顾路工牌 `GL-0606`。
  - 将“真实工作台”命名和机制调整为“工牌复核视图”，表示退出顾路账号后通过工牌进入的回收/复核层。
  - 更新 `线索.md` 状态字段：加入 `guAvatarMenuOpened`、`loggedOutFromGuAccount`、`badgeLoginPageOpened`、`badgeRecoveryViewUnlocked`。
  - 同步调整短剧钩子，把“你看到的是哪一层？”改为“退出以后，才轮到工牌说话。”。
- 目的：让剧情交互符合用户设想：玩家先看顾路工作台，主动退出后才出现工牌登录新界面。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `剧本.md`、`线索.md`、`短剧.md`，确认已出现“顾路工作台”“退出登录”“工牌登录”“工牌复核视图”等新结构关键词。
  - 搜索正文，确认新版本正文不再使用“添加页工牌审核入口”作为主线入口。
- 状态：待提交推送。

### 31. 实现退出登录与工牌登录复核视图

- 文件：
  - `workbench.html`
  - `credential-login.html`
  - `login.html`
  - `进度.md`
- 操作：
  - 在 `workbench.html` 增加顾路账号头像菜单，支持点击右上角或侧边栏头像展开账号菜单。
  - 点击“退出登录”后跳转到新增页面 `credential-login.html`。
  - 新增 `credential-login.html`，提供“密码登录 / 工牌登录”两种方式。
  - 密码登录输入 `0606` 会回到正常顾路工作台。
  - 工牌登录支持拖入顾路工牌 HTML 或 Base64 文本，校验 `GL-0606`、`顾□□□`、`南洄科技有限公司`、`Recovered Staff Credential`。
  - 工牌校验通过后进入 `workbench.html?mode=recovery`。
  - `workbench.html` 支持 `mode=recovery`，进入血色恐怖风格的工牌复核视图，并替换顶部身份、banner、状态栏等文案。
  - `login.html` 正常旧口令登录时写入 `nh_workbench_mode = normal`，确保回到原本工作台。
- 目的：先实现用户要求的功能闭环：正常登录 -> 退出登录 -> 新登录页 -> 密码回正常工作台 / 工牌进血色复核工作台。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `login.html`、`workbench.html`、`credential-login.html`，确认存在 `credential-login.html`、`mode=recovery`、`nh_workbench_mode`、`退出登录`、`工牌登录` 等关键逻辑。
  - 本地静态服务器 `http://127.0.0.1:4181/login.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html?mode=recovery` 返回 200。
- 状态：待提交推送。

### 32. 优化退出菜单与凭证登录页显示

- 文件：
  - `workbench.html`
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 修正侧边栏顾路账号菜单宽度和定位，避免菜单被页面左侧裁切导致文字显示不全。
  - 保留顶部头像菜单的完整宽度，不影响桌面端顶部账号入口。
  - 精简 `credential-login.html`，删除退出后登录页左侧大标题和说明区，只保留血色风格的登录卡。
  - 将状态输出移动到登录卡底部，保留必要的校验反馈。
- 目的：按用户截图反馈修复显示不全，并让退出后的页面更像登录界面而不是宣传/说明页。
- 验证：
  - `git diff --check` 通过。
  - 检查 `credential-login.html`，确认已删除左侧说明面板，只保留登录卡和底部状态反馈。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html?mode=recovery` 返回 200。
- 状态：待提交推送。

### 33. 隐藏账号菜单中的凭证说明文字

- 文件：
  - `workbench.html`
  - `进度.md`
- 操作：
  - 隐藏账号菜单中 `GL-0606 / 凭证待回收` 和 `退出后进入凭证复核` 这类解释文字。
  - 菜单只保留账号名和“退出登录”操作，避免入口提示过于直白。
- 目的：按用户反馈去掉截图中的文字解释，让线索更隐晦。
- 验证：
  - `git diff --check` 通过。
  - 检查 `workbench.html`，确认 `.account-card span` 已设置为 `display: none`。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html` 返回 200。
- 状态：待提交推送。

### 34. 凭证登录页改为仅工牌登录并增加退出确认

- 文件：
  - `credential-login.html`
  - `workbench.html`
  - `进度.md`
- 操作：
  - 删除 `credential-login.html` 中的“密码登录 / 工牌登录”切换。
  - 删除退出后页面的旧口令输入和“返回顾路工作台”按钮。
  - 凭证登录页只保留工牌登录区域和必要状态反馈。
  - 在 `workbench.html` 的退出登录逻辑中加入确认弹窗，确认后才跳转到凭证登录页。
- 目的：按用户要求，退出后只能刷工牌，不再通过密码回到顾路工作台，并避免误触退出。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html`，确认已无“密码登录”“旧系统口令”“返回顾路工作台”“passwordBtn”等旧密码入口残留。
  - 搜索 `workbench.html`，确认退出逻辑已加入 `window.confirm` 确认提醒。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
- 状态：待提交推送。

### 35. 调整隐藏工作台为低饱和悬疑风格

- 文件：
  - `workbench.html`
  - `进度.md`
- 操作：
  - 降低 `recovery-mode` 的红色饱和度和亮度。
  - 将高刺激血红改为低饱和暗红、灰粉、档案棕红的组合。
  - 减弱扫描线透明度和红色光晕强度。
  - 保留悬疑异常感，同时提升长时间观看舒适度。
- 目的：按用户反馈优化隐藏工作台视觉，让恐怖悬疑更协调、不刺眼。
- 验证：
  - `git diff --check` 通过。
  - 检查 `workbench.html`，确认 `recovery-mode` 已改为低饱和暗红色系，扫描线透明度降为 `0.08`。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html?mode=recovery` 返回 200。
- 状态：待提交推送。

### 36. 工牌登录页加入 TMP 临时工牌彩蛋识别

- 文件：
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 在工牌登录解析流程中加入 TMP 临时工牌分支。
  - 支持从明文 HTML 或 Base64 解密文本中识别 `TMP`、`TMP-CITY`、`临时工牌`、`临时员工`、`临时凭证` 等标记。
  - 尝试从工牌文本、HTML 属性、标题和正文中提取员工姓名。
  - 识别到 TMP 后不进入顾路复核工作台，而是在当前页面展开“临时工牌结局：旁观者编号”彩蛋画面。
  - 写入 `localStorage.nh_tmp_easter_egg`、`nh_tmp_name`、`nh_tmp_id`，便于后续剧情继续使用。
- 目的：让 TMP 临时工牌拥有独立彩蛋反馈，并能使用工牌内的员工姓名生成个性化画面。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html`，确认已加入 `parseTmpCredential`、`extractTmpName`、`tmpReveal`、`nh_tmp_easter_egg` 等 TMP 彩蛋逻辑。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
- 状态：待提交推送。

### 37. 增加工牌粒子重组转场与门禁路线回放

- 文件：
  - `credential-login.html`
  - `workbench.html`
  - `进度.md`
- 操作：
  - 在顾路工牌登录成功后加入 Canvas 粒子转场。
  - 粒子先从页面中心散开，再重组成 `GL-0606`。
  - 转场结束后跳转到 `workbench.html?mode=recovery&route=1`。
  - 在隐藏工作台复核模式中加入 SVG 门禁路线覆盖层。
  - 复核模式打开后自动绘制红色路线，并显示 `C-OLD-0606 / 15:02` 标签。
- 目的：实现用户提出的“工牌登录成功 -> 工牌粒子碎裂飞行 -> 粒子重组成 GL-0606 -> 镜头推进到血色工作台 -> 门禁路线红线自动绘制”的第一版网页动效链。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html` 和 `workbench.html`，确认已加入 `particle-stage`、`playBadgeParticleTransition`、`route-overlay`、`route-path`、`C-OLD-0606` 等动效结构。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html?mode=recovery&route=1` 返回 200。
  - 使用 Playwright 截图检查 `workbench.html?mode=recovery&route=1`，确认血色工作台和门禁路线覆盖层正常显示，未造成主要界面不可读。
- 状态：待提交推送。

### 38. 移除门禁路线回放并放慢粒子转场

- 文件：
  - `credential-login.html`
  - `workbench.html`
  - `进度.md`
- 操作：
  - 移除隐藏工作台中的门禁路线红线覆盖层和 `C-OLD-0606 / 15:02` 标签。
  - 工牌登录成功后不再跳转到 `route=1`，只进入 `workbench.html?mode=recovery`。
  - 将 Canvas 粒子转场时长从约 2.1 秒放慢到约 3.6 秒。
  - 将粒子重组完成后的停留时间略微延长，让 `GL-0606` 重组更清楚。
- 目的：按用户反馈去掉门禁路线回放，并让粒子碎裂飞行重组更慢、更有观看感。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html` 和 `workbench.html`，确认已无 `route-overlay`、`route-path`、`C-OLD-0606 / 15:02`、`route=1` 残留。
  - 检查 `credential-login.html`，确认粒子转场时长已改为 `3600ms`。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
  - 本地静态服务器 `http://127.0.0.1:4181/workbench.html?mode=recovery` 返回 200。
- 状态：待提交推送。

### 39. 粒子转场改为多形态并点击进入系统

- 文件：
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 将粒子目标从单行 `GL-0606` 改为 `GL-0606` 和“进入系统”两行文字。
  - 粒子动画改为散开、旋转环绕、波浪飞行、聚合四段。
  - 将粒子数量提升到桌面约 560 个、移动端约 280 个。
  - 将动画时长延长到约 5.6 秒。
  - 移除自动跳转，粒子重组完成后显示“进入系统”按钮，点击后进入隐藏工作台。
- 目的：按用户要求让粒子有更多形态变化，并由玩家点击进入隐藏工作台。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html`，确认存在 `particle-enter`、`is-ready`、`5600`、`进入系统` 等新动效逻辑。
  - 确认不再自动跳转，进入隐藏工作台只绑定在“进入系统”按钮点击事件上。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
- 状态：待提交推送。

### 40. 优化粒子重组可读性

- 文件：
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 调整粒子重组目标，只让粒子组成 `GL-0606`。
  - 将“进入系统”从粒子点阵改为清晰按钮浮现，避免中文字笔画过多导致画面混乱。
  - 提高粒子数量，让 `GL-0606` 点阵更密、更清楚。
- 目的：按用户截图反馈优化粒子最终画面，减少杂乱感，提高可读性。
- 补充操作：
  - 按用户修正，将粒子重组画面改为两行：`GL-0606` 和 `出入平安`。
  - 将点击控件文案保留为“进入系统”。
- 验证：
  - `git diff --check` 通过。
  - 搜索 `credential-login.html`，确认粒子目标包含 `GL-0606` 和 `出入平安`，点击控件保留“进入系统”。
  - 本地静态服务器 `http://127.0.0.1:4181/credential-login.html` 返回 200。
- 状态：待提交推送。

### 41. 推送工牌登录与隐藏工作台版本

- 文件：
  - `进度.md`
- 操作：
  - 检查当前 Git 状态。
  - 确认最新本地提交为 `abebacc Add credential badge login flow`。
  - 记录本次准备推送到 GitHub 的操作。
- 目的：按用户要求将当前版本推送到 GitHub，并保持操作记录完整。
- 状态：准备提交推送。

### 42. 再次尝试推送 GitHub

- 文件：
  - `进度.md`
- 操作：
  - 检查 Git 状态，确认本地 `main` 分支领先远端 2 个提交。
  - 记录用户再次要求推送 GitHub 的操作。
- 目的：继续尝试将工牌登录、隐藏工作台与文档更新推送到远端仓库。
- 状态：准备提交推送。

### 43. 第三次尝试推送 GitHub

- 文件：
  - `进度.md`
- 操作：
  - 检查 Git 状态，确认本地 `main` 分支领先远端 3 个提交。
  - 记录用户继续要求推送 GitHub 的操作。
- 目的：继续尝试将当前全部本地提交推送到远端仓库。
- 状态：准备提交推送。

### 44. 排查 GitHub 推送 TLS 问题

- 文件：
  - `进度.md`
- 操作：
  - 检查远端地址，确认 `origin` 使用 `https://github.com/nmwllwmn/keji.git`。
  - 检查 Git 配置，确认当前 SSL 后端为 `schannel`。
  - 准备切换 TLS 后端后再次尝试推送。
- 目的：定位 GitHub 推送失败原因，排除本机 Git TLS 配置问题。
- 状态：排查中。

### 45. 排查 VS Code 网页预览能力

- 文件：
  - `进度.md`
- 操作：
  - 检查本机 VS Code 命令行是否可用。
  - 检查当前已安装扩展，确认缺少常用网页预览与 Live Server 扩展。
  - 尝试安装 `ritwickdey.LiveServer`、`ms-vscode.live-server` 与编辑器预览相关扩展。
  - 确认 `bierner.lit-html` 安装成功，但两个 Live Server 扩展因 TLS 连接中断安装失败。
- 目的：解决 HTML 页面无法在 VS Code 内直接预览的问题。
- 状态：处理中。

### 46. 安装 VS Code 网页预览插件并补充工作区配置

- 文件：
  - `.vscode/extensions.json`
  - `.vscode/settings.json`
  - `进度.md`
- 操作：
  - 通过离线下载 `.vsix` 的方式安装 `ritwickdey.LiveServer`。
  - 确认 `ms-vscode.live-server` 与当前 VS Code 版本不兼容，放弃安装。
  - 为项目新增 VS Code 工作区推荐扩展与 Live Server 配置，便于后续直接预览 HTML。
- 目的：让当前网页项目可以在 VS Code 中直接启动本地预览。
- 状态：已完成，待提交。

### 47. 制作 TMP 临时工牌独立彩蛋页

- 文件：
  - `tmp-easter-egg.html`
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 读取用户提供的 `code (2).html`，确认其为萤火粒子氛围页面。
  - 新增 `tmp-easter-egg.html`，将萤火粒子动画优化为 TMP 临时凭证专属彩蛋界面。
  - 增加 TMP 姓名、编号、准入池、留痕状态展示。
  - 增加返回工牌入口与清空临时留痕按钮。
  - 修改 `credential-login.html`，识别到 TMP 工牌后写入本地状态并跳转到独立彩蛋页。
- 目的：让临时工工牌登录显示更美观、高端、完整且可运行的专属画面，同时不影响顾路主线。
- 状态：待验证。

### 48. 修正 TMP 工牌 Base64 解密与字段识别

- 文件：
  - `credential-login.html`
  - `进度.md`
- 操作：
  - 按用户补充的加密解密规则，明确支持 `UTF-8 HTML -> Base64` 的工牌文件。
  - 优化 Base64 归一化，兼容换行、空格、URL-safe Base64 与缺失补位。
  - 从解密后的 HTML/文本中提取员工姓名、工号、员工编号、临时编号、ID 等字段。
  - 只有识别到 `TMP` 工号或临时工牌识别属性时，才触发 TMP 彩蛋页。
- 目的：确保临时工牌必须解出姓名与工号，并确认属于 TMP 临时工后才进入彩蛋。
- 验证：
  - 使用 `UTF-8 HTML -> Base64` 的模拟 TMP 工牌测试，成功识别 `姓名=林舟`、`工号=TMP-0617`、`mode=Base64 UTF-8 解密`。
  - 使用非 TMP 工号的 Base64 工牌测试，确认不会误触发 TMP 彩蛋。
  - `credential-login.html` 与 `tmp-easter-egg.html` 本地静态服务均返回 200。
  - `git diff --check` 通过。
- 状态：已完成，待提交。

### 49. 补充 TMP 工牌测试文件

- 文件：
  - `tmp-badge-test.html`
  - `tmp-badge-test-base64.txt`
  - `进度.md`
- 操作：
  - 新增一份明文 TMP 工牌 HTML 测试页，包含姓名、工号与 TMP 识别属性。
  - 根据 `UTF-8 -> Base64` 规则生成对应的 Base64 测试文本文件。
- 目的：提供可直接拖入工牌登录页的 TMP 工牌测试样本，便于验证彩蛋触发逻辑。
- 状态：已完成，待提交。

### 50. 适配真实 TMP 工牌内部协议

- 文件：
  - `credential-login.html`
  - `tmp-real-lishugen-9528.html`
  - `进度.md`
- 操作：
  - 读取用户提供的 `工牌_李树根_9528.html`。
  - 确认该文件外层为工牌查看器，真实工牌 HTML 存在于内部 Base64 变量中，并通过 `_d()` 解密后 `document.write()`。
  - 新增内部 Base64 候选扫描与递归解密逻辑，支持从 HTML 文件内部解出真实工牌内容。
  - 调整 TMP 姓名提取优先级，避免误把 `viewport` 等元信息当作姓名。
  - 将真实工牌复制为 `tmp-real-lishugen-9528.html` 作为本地测试样本。
- 验证：
  - 使用真实工牌样本测试，成功识别 `姓名=李树根`、`工号=TMP-9528`、`mode=明文 HTML / 内部Base64解密`。
- 目的：只要工牌 HTML 内部按协议解密后是 TMP 临时工，就触发 TMP 彩蛋页。
- 状态：已完成，待提交。

### 51. 优化 TMP 彩蛋页文案与感谢语显示

- 文件：
  - `tmp-easter-egg.html`
  - `进度.md`
- 操作：
  - 将首屏大标题优化为“临时权限止步于正式系统之外”。
  - 调整说明文案，强调临时工牌可通过门禁，但无法进入顾路正式回收池或主线权限。
  - 将粒子最终文字改为“感谢您对游戏的支持”。
  - 优化粒子文字采样字号与密度，适配长中文句子。
  - 增加清晰浮现的感谢语层，避免粒子点阵过散导致阅读不清。
- 目的：让 TMP 彩蛋页更美观，同时明确表达临时工没有进入真正系统权限的含义。
- 状态：待验证。

### 52. TMP 粒子文字加入员工名和 ID

- 文件：
  - `tmp-easter-egg.html`
  - `进度.md`
- 操作：
  - 将粒子最终目标改为多行文本：感谢语、员工名、员工 ID。
  - 将清晰浮现的感谢语层同步增加 `NAME / 员工名 · ID / 编号`。
  - 调整粒子文字采样函数，支持主标题与元信息使用不同字号。
  - 为 TMP 彩蛋页增加 `?name=员工名&id=编号` 预览参数，便于直接查看指定工牌效果。
- 目的：让 TMP 彩蛋最终画面明确显示当前工牌对应的员工名和 ID。
- 验证：
  - 使用 `?name=李树根&id=TMP-9528` 截图检查，首屏员工名和 ID 显示正确。
- 状态：已完成，待提交。

### 53. 优化 TMP 最终粒子画面协调性

- 文件：
  - `tmp-easter-egg.html`
  - `进度.md`
- 操作：
  - 将最终感谢语从巨大裸文字改为居中的半透明玻璃徽章。
  - 将员工名和 ID 拆成两个小标签，降低信息压迫感。
  - 降低粒子光晕亮度、缩小粒子点阵字号，让粒子作为氛围层而不是抢占主体。
  - 调整粒子采样密度与最终文字宽度，避免“感谢语”和员工信息互相挤压。
- 目的：让 TMP 彩蛋最终画面更协调、精致，减少字幕压屏和光效过曝。
- 状态：待验证。

### 54. 提交并推送 TMP 彩蛋功能

- 文件：
  - `credential-login.html`
  - `tmp-easter-egg.html`
  - `tmp-badge-test.html`
  - `tmp-badge-test-base64.txt`
  - `tmp-real-lishugen-9528.html`
  - `.vscode/extensions.json`
  - `.vscode/settings.json`
  - `进度.md`
- 操作：
  - 检查 Git 状态，确认当前有 TMP 彩蛋、工牌解析、测试工牌与 VS Code 配置相关改动。
  - 准备提交并推送到 GitHub。
- 目的：按用户要求将当前可用版本同步到远端仓库。
- 状态：准备提交推送。

### 55. 调整普通工作台表层为无异常状态

- 文件：
  - `workbench.html`
  - `进度.md`
- 操作：
  - 将普通顾路工作台首页改为表层正常状态：消息、待办、审批均显示为 0 或已完成。
  - 隐藏普通模式下的侧边栏红点、消息角标、待办角标、通知红点与待办优先级点。
  - 将普通模式中的待办、审批、日程、公告文案改为日常办公内容。
  - 保留恢复模式内容，刷入 GL 工牌进入 `?mode=recovery` 后再显示异常消息、待办、DING 与复核残留。
  - 将退出登录改为确认后打开新的 `credential-login.html` 窗口，而不是当前工作台页面内跳转。
- 目的：符合剧情设定，玩家首次进入顾路工作台时不应从首页看出任何披露或异常。
- 验证：
  - `workbench.html`、`workbench.html?mode=recovery` 与 `credential-login.html` 本地静态服务均返回 200。
  - 检查代码确认退出登录使用 `window.open('credential-login.html', '_blank', 'noopener,noreferrer')`。
  - 截图检查普通工作台首页，确认消息、待办、审批无红点与异常角标，首页内容为正常办公状态。
  - `git diff --check` 通过。
- 状态：已完成，待提交推送。

### 56. 补齐工作台所有应用入口与合理页面

- 文件：
  - `module-shell.html`
  - `workbench.html`
  - `messages.html`
  - `documents.html`
  - `contacts.html`
  - `calendar.html`
  - `进度.md`
- 操作：
  - 将 `module-shell.html` 从占位页改为统一的南洄工作台应用外壳。
  - 为消息、文档、AI 表格、AI 听记、通讯录、会议、日历、待办、DING、访客记录、设备归还、归档、添加应用分别制作可打开的真实模块界面。
  - 消息页改为会话列表加聊天框，保持普通表层无红点、无异常未读。
  - 文档页增加 Word、Excel、PPT、PDF 文件列表，并显示创建人、最近打开时间、同步状态和打开操作。
  - 通讯录、日历、待办、DING、会议、设备、访客、归档等页面补齐符合企业工作台逻辑的列表、表格、卡片与操作控件。
  - 将 `workbench.html` 中消息、文档、通讯录、日历入口统一改为 `module-shell.html?module=...`，继续保持新窗口打开。
  - 将旧的 `messages.html`、`documents.html`、`contacts.html`、`calendar.html` 改为兼容跳转页，避免旧链接失效。
- 目的：满足所有控件应用都能打开且打开后合理可用的要求，同时保持当前南洄工作台 UI 风格不变。
- 验证：
  - `module-shell.html` 内全部渲染函数静态检查通过。
  - 使用 Chrome headless 对 13 个模块生成截图，确认页面均可渲染且不是空白。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 57. 新增 AI小洄悬浮记事本

- 文件：
  - `module-shell.html`
  - `workbench.html`
  - `进度.md`
- 操作：
  - 在工作台应用分支中新增 `AI小洄`，定位为 AI 记事本与复核摘录助手。
  - 在 `module-shell.html` 中新增 `aiXiaohui` 应用配置与 `renderXiaohui()` 渲染函数。
  - 将 `AI小洄` 的核心交互改为右侧悬浮记事本，默认收起，只露出竖向 `AI小洄` 标签。
  - 悬浮窗支持展开、收起、自动保存、本地持久化和一键插入摘录。
  - 在 `workbench.html` 与所有 `module-shell.html` 应用分支中都加入同一个悬浮记事窗，让玩家打开消息、文档、访客记录等页面时也能边查边记。
  - 保留 `module-shell.html?module=aiXiaohui` 作为完整笔记页，结构采用左侧笔记列表、中间笔记正文、右侧小洄摘要面板。
  - 内容设计为普通视图下合理的复核草稿、材料摘录、待核对片段，不直接透露真相，保留后续分支扩展空间。
  - 在 `workbench.html` 侧边栏、常用应用、全部应用中加入 `AI小洄` 入口，仍保持新窗口打开。
- 目的：把记事本功能以悬浮窗形式融入游戏工作台，让它成为后续线索整理、应用分支和玩家复核记录的随身载体。
- 验证：
  - `module-shell.html` 脚本语法检查通过。
  - 使用 Chrome headless 截图检查 `module-shell.html?module=aiXiaohui`，确认 AI小洄页面渲染正常。
  - 使用 Chrome headless 截图检查 `workbench.html`，确认首页与侧边栏入口显示协调。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 58. 简化 AI小洄为可拖拽文本框

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将 AI小洄悬浮窗简化为纯记事文本框，移除摘录按钮和“打开完整笔记”入口。
  - 保留标题栏、当前视图标签、文本框和自动保存状态，让它更像随手记事本。
  - 增加标题栏拖拽移动功能，玩家可把 AI小洄放到右侧任意位置。
  - 开启右下角拖拽缩放，玩家可自由调整悬浮窗宽高。
  - 将 AI小洄的文本内容、展开状态、位置和大小保存到本地存储，跨页面保留。
  - 在隐藏工作台 `recovery-mode` 中将 AI小洄切换为暗红色风格，配合血色工作台氛围。
- 目的：让 AI小洄作为游戏内常驻记事本工具存在，而不是一个复杂应用页面。
- 状态：已完成，待验证提交。

### 59. 完善 AI小洄收起、拖拽与四向缩放

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将 AI小洄收起逻辑从横向位移改为真实压缩面板，只保留固定 42px 宽的侧边标签。
  - 展开时恢复上一次完整位置、宽度和高度。
  - 拖拽到任意位置后，收起不再回到右侧，而是在当前位置缩成侧边标签。
  - 增加四边和四角共 8 个缩放热区，支持从任意边框和角落拉大缩小。
  - 保持侧边标签占据大小不变，展开/收起状态、位置和大小继续本地保存。
- 目的：让 AI小洄更像一个可自由摆放的桌面记事本，而不是固定侧栏组件。
- 状态：已完成，待提交推送。

### 60. 填充普通工作台应用内容与详情弹窗

- 文件：
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 在普通顾路工作台应用外壳中新增统一详情弹窗，所有详情保持企业工作台风格，不破坏表层正常感。
  - 为消息、文档、AI 表格、AI 听记、通讯录、会议、日历、待办、DING、访客记录、设备归还、归档审批补齐可点击详情。
  - 文档列表中的 Word、Excel、PPT、PDF 均可打开对应预览说明，内容围绕离职交接、设备台账、网络认证、访客登记、周报、安全手册展开。
  - 消息、DING、待办、AI 听记采用“已签收、已归档、已生成、无异常”的表层口吻，符合最坏结局前的正常视图设定。
  - 访客记录、设备归还、归档审批中保留顾路、GL-0606、门禁工牌、普通回执、恢复态等轻微可回看信息，但不在首页制造明显红点或异常。
  - 详情弹窗增加“记录到 AI小洄”操作，可把当前详情标题追加到悬浮记事本，便于后续线索整理玩法扩展。
  - 上传、新建、筛选、导出、扫码入库、批量归档等按钮均接入合理详情，不再是无反馈控件。
- 目的：把普通工作台从可打开页面推进到可游玩的表层证据系统，让玩家能在“看起来一切正常”的界面中完成初步复核并被误导。
- 验证：
  - `module-shell.html` 内脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
  - 使用 Chrome headless 截图检查消息、文档、AI 听记、访客、设备、归档 6 个模块，确认页面正常渲染且风格统一。
  - 尝试使用 Playwright 包做 DOM 点击验证，但当前项目未安装 `playwright` 依赖，未向项目新增依赖。
- 状态：已完成，待提交推送。

### 61. 二次充实普通工作台文案与首页控件

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将普通工作台文档列表改为更贴合剧本的核心文件：`离职交接清单_顾路_归档版`、`设备补交登记说明`、`绩效申诉处理记录_已沟通`。
  - 为 `设备补交登记说明` 和 `绩效申诉处理记录_已沟通` 增加独立详情内容，补齐设备补交、绩效申诉、服务台结论等表层证据。
  - AI 表格增加“绩效申诉”工作流记录，使普通视图的流程闭环更完整。
  - AI小洄完整页移除不属于企业工作台内部的家属访谈内容，改为申诉记录摘录、普通摘要规则、字段展示范围等复核笔记。
  - 首页顶部“消息 / 文档 / 通讯录”标签改为打开对应模块的新窗口。
  - 首页通知铃、设置按钮、公告通知加入普通视图详情弹窗。
  - 首页待办、审批、日程相关行改为打开对应模块，不再只是切换勾选状态。
- 目的：让普通顾路工作台更像一套真实可查的企业系统，而不是只在弹窗中解释剧情；玩家可以自然地从首页进入各应用分支，看到“表层正常”的完整证据包。
- 验证：
  - `workbench.html` 和 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
  - 使用 Chrome headless 截图检查 `workbench.html`，确认首页正常渲染、普通视图无红点干扰。
  - 文档页截图验证命令曾因 PowerShell 路径并行清理冲突失败，页面脚本静态检查通过，未引入项目依赖。
- 状态：已完成，待提交推送。

### 62. AI小洄吸附侧边栏

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 为首页和各模块页的 AI小洄悬浮窗加入左右边缘吸附逻辑。
  - 拖拽松手后，悬浮窗会自动停靠到更靠近的一侧边缘，避免漂在页面中部。
  - 收起状态会固定成贴边侧边栏标签，展开后恢复最近一次记录的尺寸和位置，并继续保持吸附侧边的习惯。
  - 保留原有拖拽、缩放、本地保存能力，不影响记事本功能和隐藏工作台的红色风格。
- 目的：让 AI小洄更像真正附着在工作台边缘的便签工具，操作更干净，也更符合当前界面的企业桌面感。
- 验证：
  - `workbench.html` 和 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 63. 文档与记录详情改为真实应用界面

- 文件：
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将原来的通用文字详情改为按类型渲染的真实界面。
  - Word / PDF 类文件打开后显示为带工具栏和纸张版心的正文预览。
  - Excel 类文件打开后显示为带公式栏、工作表名、行号列标和网格线的表格界面。
  - PPT 类文件打开后显示为左侧缩略图加右侧 16:9 幻灯片画布。
  - 消息和 DING 打开后显示为聊天气泡界面。
  - 访客、归档、网络认证等记录打开后显示为时间线界面。
  - 设备、账号等台账打开后显示为字段表界面。
  - 通讯录打开后显示为人员资料卡界面。
  - 待办打开后显示为勾选清单界面，会议和日历打开后显示为日程时间线。
  - 加宽详情窗口，避免 Excel / PPT 预览过窄。
- 目的：让“打开文件/打开记录”真的像进入对应办公应用，而不是弹出一段说明文字。
- 验证：
  - `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
  - 结构检查确认页面包含 `excel-grid`、`word-page`、`ppt-slide` 等真实预览节点。
- 状态：已完成，待提交推送。

### 64. 消息模块改为可切换聊天框

- 文件：
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将消息模块重构为左侧会话列表 + 右侧聊天框的真实结构。
  - 每个会话都支持点击切换，右侧会话标题、成员、消息内容随之变化。
  - 为行政通知、技术一组、人事服务台、郑明、系统助手补充完整对话内容。
  - 聊天输入框支持回车发送本地临时消息，保持可用状态。
  - 保留普通工作台表层正常感，不在消息页直接暴露隐藏真相。
- 目的：让消息模块看起来就像真实的企业聊天界面，点开每一条会话都能继续玩，而不是只进一段说明页。
- 验证：
  - `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 65. 继续把各功能模块做成真实办公应用

- 文件：
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将会议模块重做为会议室排期 + 今日安排 + 右侧详情侧栏。
  - 将待办模块改为三列看板，区分待处理、处理中、已完成。
  - 将 DING 模块改为通知列表 + 签收人面板，不再只是卡片堆叠。
  - 将访客记录改为登记台账 + 门禁联动地图 + 过滤标签。
  - 将设备归还改为资产台账 + 回执预览侧栏。
  - 将归档与审批改为流程中心 + 复核报告 + 审批侧栏。
  - 将 AI 表格、AI 听记、通讯录继续改成更接近真实办公系统的三栏结构与详情面板。
- 目的：把剩余偏“表格壳子”的模块继续补齐，尽量做到每个菜单点开后都像真的办公应用，而不是同一套列表样式换标题。
- 验证：
  - `workbench.html` 与 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 66. 调整 AI小洄吸附与待办路线表达

- 文件：
  - `workbench.html`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 将 AI小洄改成可拖拽移动的悬浮记事本，松手后自动吸附到左右边缘。
  - 收起状态下的小标签也允许拖拽，并在松手后继续吸附，保留当前边侧位置。
  - 将待办模块改写为顾路自己的行动痕迹，区分“准备做 / 做过的 / 已确认”三列。
  - 在待办里加入文档、消息、访客、设备、工牌复核等路线痕迹，但只作为引导，不直接写成攻略。
  - 为待办详情补充更贴合顾路的清单项和复核项。
- 目的：让 AI小洄更像真正可摆放的桌面便签，并让待办承担“玩家游玩路线引导”的角色，而不是直接泄题。
- 验证：
  - `workbench.html` 与 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 67. 优化文档窗口与正常工作台坏结局路线

- 文件：
  - `module-shell.html`
  - `剧本.md`
  - `进度.md`
- 操作：
  - 将详情弹窗改成更像桌面文档窗口的形态，打开后只呈现对应文档、表格、PPT、聊天或记录内容。
  - 移除“已阅”和“记录到 AI小洄”等出戏操作。
  - 为文档窗口增加关闭、放大/还原、拖拽移动和浏览器原生尺寸缩放能力。
  - 优化普通工作台第一层证据文本，让文档、听记、访客、设备、归档报告形成“看起来正常”的坏结局路线。
  - 在 `剧本.md` 中新增“正常工作台坏结局游玩路线”，明确待办如何作为顾路自己的行动痕迹来引导玩家，而不是直接写攻略。
- 目的：先完成正常工作台的坏结局整体路线，让玩家顺着表层证据能够自然草率结案，同时为后续工牌复核视图形成反差。
- 验证：
  - `workbench.html` 与 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 68. 将正常工作台坏结局改为链路式解密路线

- 文件：
  - `线索.md`
  - `剧本.md`
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 明确坏结局不是控件按钮触发，而是玩家按顺序走完顾路待办、文档、消息、AI 听记、访客、设备、日历、DING、报告预览后自然误判。
  - 将正常工作台坏结局整理为链路式线索，保留 30-40 条核心推进点。
  - 把待办改写成顾路自己的行动痕迹，作为玩家进入下一层内容的自然引导。
  - 将详情窗口继续收紧为“对应文档本体”，仅保留关闭、放大/还原、拖拽、缩放。
  - 修正剧本里“直接签字触发”一类表述，改为“顺着表层证据确认报告”。
- 目的：让普通工作台坏结局真正变成一条一层层解密的线路，而不是其他控件直接触发的结果。
- 验证：
  - `workbench.html` 与 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 69. 统一每个工作台页面左下角退出登录入口

- 文件：
  - `module-shell.html`
  - `进度.md`
- 操作：
  - 为所有 `module-shell.html` 分支页补上左下角账号菜单。
  - 账号菜单包含当前账号信息和“退出登录”。
  - 退出时弹出确认框，确认后新窗口打开 `credential-login.html`。
  - 保持主工作台原有左下角退出入口不变，并与模块页行为一致。
- 目的：让每个工作台页面都可以从左下角退出回工牌登录，不只限于主工作台。
- 验证：
  - `workbench.html` 与 `module-shell.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。

### 70. 编写表面工作台坏结局与表面彩蛋设计

- 文件：
  - `表面坏结局.md`
  - `剧本.md`
  - `线索.md`
  - `进度.md`
- 操作：
  - 新增 `表面坏结局.md`，专门整理顾路普通工作台第一层的坏结局路线。
  - 明确表面坏结局不是按钮触发，而是玩家沿着顾路待办、文档、消息、AI 听记、访客、设备、日历、DING、报告预览一步步形成错误判断。
  - 将表面路线整理为约 40 条核心线索，保证内容符合普通工作台的逻辑。
  - 设计表面彩蛋结局小游戏 `周报封面拼版`：玩家拖拽办公封面字段，拼出一张完整归档封面，获得轻量彩蛋反馈。
  - 表面彩蛋不透露真相、不影响主线正式结局，只作为普通工作台里的办公系统隐藏小玩法。
- 目的：先把正常工作台的表面坏结局和表面彩蛋结局设计成可落地文档，后续再按文档实现页面与交互。
- 验证：
  - `git diff --check` 通过，仅有 Windows 换行提示。
  - 检查 `表面坏结局.md`，确认包含表面坏结局、30-40 条线索路线、表面彩蛋结局和小游戏设计。
- 状态：已完成，待提交推送。

### 71. 实现三层周报封面滑块拼图彩蛋

- 文件：
  - `weekly-cover-puzzle.html`
  - `module-shell.html`
  - `表面坏结局.md`
  - `进度.md`
- 操作：
  - 新增 `weekly-cover-puzzle.html`，实现“周报封面预览”三层闯关小游戏。
  - 第一关为 `4 x 4`，第二关为 `6 x 6`，第三关为 `8 x 8`。
  - 每关只有一个空位，玩家点击空位相邻切片即可移动，玩法符合常见滑块拼图。
  - 使用一张办公风格的周报封面作为拼图图像，内容包含公司、周报封面、顾路、GL-0606、版本号、归档编号等字段。
  - 三关完成后写入 `localStorage.nh_surface_cover_puzzle_done = true`，并给 AI小洄追加“封面比正文更整齐”的轻量彩蛋摘录。
  - 在应用中心新增“周报封面预览”入口。
  - 同步更新 `表面坏结局.md`，将原拖拽卡片小游戏改为三层滑块拼图设计。
- 目的：让表面彩蛋结局变成一个贴合办公系统的实际小游戏，同时不影响主线证据和正式结局。
- 验证：
  - `workbench.html`、`module-shell.html`、`weekly-cover-puzzle.html` 脚本静态语法检查通过。
  - `git diff --check` 通过，仅有 Windows 换行提示。
- 状态：已完成，待提交推送。
